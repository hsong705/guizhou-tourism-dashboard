{
  "name": "guizhou-tourism-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_BASE=/guizhou-tourism-dashboard/ vite build && touch dist/.nojekyll && gh-pages -d dist"
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "sass": "^1.100.0",
    "vue": "^3.5.34",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.12"
  }
}
